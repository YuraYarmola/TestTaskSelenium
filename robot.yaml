#tasks:
#  Run all my tasks:
#    shell: python -m robocorp.tasks run try_robocorp.py
#
#
tasks:
  Default:
    actions:
      - Run the main task

condaConfigFile: conda.yaml
artifactsDir: output

libraries:
  - RPA.Browser.Selenium