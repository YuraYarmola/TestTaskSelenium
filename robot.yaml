#tasks:
#  Run all my tasks:
#    shell: python -m robocorp.tasks run try_robocorp.py
#
#
tasks:
  Default:
    shell: python -m robocorp.tasks run try_robocorp.py -t browser_example

condaConfigFile: conda.yaml
artifactsDir: output

libraries:
  - RPA.Browser.Selenium